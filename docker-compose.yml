version: "3.8"
services:
  web:
    build: .
    ports:
      - "80:8000"
    environment:
      - LLM_PROVIDER
      - GEMINI_API_KEY
      - GEMINI_MODEL
      - GEMINI_EMBED_MODEL
      - DATABASE_URL
